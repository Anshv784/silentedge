# --- Secrets: never commit ---
.env
.env.*
!.env.example
*.pem
*.key
id.json
*-keypair.json
*keypair*.json
wallet*.json
bls-keypair.json
identity.pem
/keys/
/.secrets/

# --- Rust / Anchor / Solana ---
target/
.anchor/
test-ledger/
**/*.rs.bk
# Cargo.lock is deliberately COMMITTED. The ignore-the-lockfile convention is
# for libraries; this workspace builds an on-chain program that holds funds,
# and the lockfile is what makes the build reproducible.

# --- Arcium ---
build/
.arcium/
artifacts/

# --- Node ---
node_modules/
dist/
.next/
out/
.vercel/
*.tsbuildinfo
.turbo/
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# --- Surfpool ---
.surfpool/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.cursor/
.claude/
.vscode/*
!.vscode/extensions.json
